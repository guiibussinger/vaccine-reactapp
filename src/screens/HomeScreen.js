import React from 'react';
import styled from 'styled-components/native';

const HomeScreen = () => <StyledHome />;

const StyledHome = styled.View`
  flex: 1;
  background-color: lightblue;
`;

export default HomeScreen;
